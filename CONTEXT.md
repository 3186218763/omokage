# omokage

虚拟人物重现。当前这一位是纪念向的「真白花音」：用户与她风格的 AI 说话，听到的声音与读到的台词应对齐同一种在场感。

## Language

### 产品

**多模态在场感**:
产品验收单位：同一轮回复里，台词内容、语气意图、合成声线三者一致，像在跟她语音聊天；不是「只像文风」或「只像音色」。
_Avoid_: 纯文风复刻, 纯声线复刻, 人格模拟（单独使用时含义不清）

**真人对话复刻**:
产品形态是与 AI 花音的实时对话（前端只暴露这一种交互），不是 TTS 试听台、不是多角色切换台、也不是让用户手调参数的声线工具。
_Avoid_: 纯 TTS 产品, 参考音试听器, 多角色扮演大厅

**AI 复刻**:
明确标注为纪念/复刻系统，不是本人、不冒充在世主播、不承诺复活或商用。
_Avoid_: 本人, 复活, 官方账号

**打断**:
用户在角色说话时开口或点击，角色立即让路：停当前语音、放弃本轮剩余输出，等用户新输入。半双工形态内的廉价档；麦克风常开的全双工不在 v1。
_Avoid_: 全双工, barge-in（对外文案统一用「打断」）

### 稳定层（项目默认，主要进 LLM）

**人格**:
她是谁：身份锚点、与用户/粉丝的关系、纪念立场、价值观与红线（含不冒充本人、不复活承诺等）。
_Avoid_: 把人格当成每句可切换的面具, 人设大包（与性格混用不加区分时）

**性格**:
稳定气质先验：多特质**同时在场**（如笨、屑、元气、温柔、倔强），约束「会怎么接话」；某一轮可以侧重不同，但不是单选面具轮换，也不是参考音标签。
_Avoid_: 用性格当每句参考音标签, 每轮只演一个特质, 性格枚举, Big Five / OCEAN 分数当角色定义

**时期**:
人设与声线的年代锚点；默认锁定清楚元气期，不是日常换参考音的主轴。
_Avoid_: 用时期当每句参考音标签

**文本语气指纹**:
写台词时的稳定表面规则：句量、口癖密度、禁列表腔/禁动作括号、中日混用习惯等。
_Avoid_: 语气（单独使用时与说话语气混淆）, 用文本规则代替参考音

### 按轮 / 按句层

**事实**:
可召回的公开背景与偏好（履历梗、食物喜好等）；只作内容依据，不假装亲历记忆。
_Avoid_: 把事实写成必须复述的台词

**场景**:
这轮对话在聊什么或处于哪种互动情境；只约束台词与事实召回，**不**直接选择参考音。
_Avoid_: 用场景当参考音索引

**说话语气**:
驱动「用哪条参考音」的主轴：这句合成语音**听起来**是什么劲儿（韵律/气质），与话题无关。v1 闭集六类：**日常**（默认一般闲聊）、**元气**、**温柔**、**俏皮**、**倔强**、**惊讶**；不含「低落」声线，沉重话题用温柔接住。
_Avoid_: 情绪（单独使用时易与心理状态混淆）, 情感, mood, 低落（v1 不做）, 用性格或场景直接当说话语气, 用说话语气直接写 Live2D 参数

**表演情绪**:
这一轮 Live2D 脸和身体的劲儿，是**持续底色**——动作叠加其上，不替代它。闭集与说话语气相同（日常、元气、温柔、俏皮、倔强、惊讶），便于回落；由 System One（本地 Kev 或托管 Jev）判定，用户不可见。发给模型的选项和说明是英文，再映射回中文闭集。未配置、超时或置信度不足时，整轮表演情绪等于该轮说话语气。沉重话题走温柔，不设低落/哭脸。
_Avoid_: 把表演情绪写进台词, 用说话语气标签当 UI 文案, 低落, 哭脸

**动作**:
离散、有起止、按句触发的身体事件（点头、摇头、歪头…），叠加在表演情绪底色之上。标签先行，不进用户可见文本、不被朗读。
_Avoid_: 动作括号（写进台词的形式）, 与表演情绪混用, 把动作当持续状态

### 控制归属（约定）

- **LLM 固定持有**：人格、性格、时期、文本语气指纹  
- **LLM 按轮**：事实、场景  
- **说话语气判定**：由 LLM 在生成时自标（闭集枚举），用户不可见、不可手选  
- **表演情绪判定**：由 System One（本地 Kev 或托管 Jev）按轮判定，英文选项映射回中文闭集；不可用时等于该轮说话语气。只驱动 Live2D，不进台词、不进 UI 文案  
- **动作判定**：由 LLM 在生成时自标（闭集枚举、按句、标签先行），用户不可见、不可手选；System One 仍只管表演情绪。不解除「禁动作括号」：括号写进台词依旧禁止并剥除，动作只走标签通道  
- **动作粒度（v1）**：按句、可有可无；缺省无动作，舞台回落表演情绪底色。动作闭集由皮套资产能播什么决定，程序化小动作兜底；被占线或节流时直接丢弃，不排队  
- **动作标签协议（v1）**：句前内联 `【动作：点头】`，与轮级说话语气标签同族语法；解析后不朗读、不进 UI。动作直接绑定对应句，可在同一轮按语义重复使用
- **动作词表（v1）**：点头、摇头、歪头三件，全程序化曲线，任意 Cubism 4 皮套可跑；占位模型自带 motion 只作播放链路的开发验证，不进词表。扩词表先扩资产或程序化曲线  
- **说话语气粒度（v1）**：整轮回复共用一个说话语气；映射到参考音后，该轮各句 TTS 共用同一参考音；缺省/非法标签回落 **日常**  
- **说话语气时序（v1）**：标签先行——先解析闭集标签，再流式输出可朗读台词；首句 TTS 即用该轮参考音；标签不得进入用户可见文本或被朗读  
- **打断触发（v1）**：仅 Web 两入口——用户点击麦克风开始录音的那一刻、显式让路控件（点舞台/停止按钮）；不做麦克风能量 VAD 触发  
- **打断归属（v1）**：被打断的回复，已播出的句子照常进对话历史，未播出的丢弃；不做「被打断」标记  
- **皮套解耦（v1）**：产品以「任意 Cubism 4 模型可跑」为目标，占位期靠程序化动作保证体验完整；baicai 皮套（含语义 motion）是并行外部线，agent 侧只交付资产规格，到货后扩词表为纯增量  
- **TTS**：说话语气 → 参考音（wav + 对应 prompt 文本）  
- **参考音库（v1）**：人听精选；每个说话语气 1 条主参考音 + 至多 2 条备选；条目含 wav 与对应转写文本；运行时按说话语气取主参考音，不在线随机跳换  
- **对话侧资产（v1）**：人格/性格等稳定层以 system prompt 为准；另须具备可加载的 **事实库** 与 **场景 few-shot 台词**；与五类说话语气参考音库共同构成多模态在场，缺一不可  
- **人格资产来源（v1）**：以仓库已有 `docs/persona/`（research / spec / facts / fewshot / 评测）为基线恢复并增量，不推倒重写；新增工作集中在说话语气参考音库与标签先行对话管线  
- **验收（v1）**：四轨——（1）文本：既有 eval-set 盲测与一票否决；（2）听感：同题合成音的说话语气合理性、像她、与台词一致；（3）交互：前端仅与 AI 花音对话，说话语气标签用户不可见，流式可聊；（4）表演：动作与台词同屏的录屏盲测，一票否决——动作与台词语义冲突、时机不在对应句播放窗口内、或长期缺失即不过；样本 20 轮 × 3 场景（日常闲聊/沉重话题/玩笑挑衅）；不以动作覆盖率为 KPI。不以 SER 分类准确率为主 KPI  
