# 打断先做廉价档：用户开口/点击即让路，全双工推迟

产品要「像真人语音聊天」，用户插话时她不停是最伤体感的一档缺口。全双工（麦克风常开、VAD 检测插话、回声消除调参）成本高且调参深；v1 决定只做廉价档：用户开始录音或点击让路控件时，淡出当前音频、清空待播合成、中止 LLM 流，半双工交互模型不变。

## Consequences

- 前端音频队列需要 flush 语义；同一会话的串行锁不复用为「不可打断」的依据。
- 全双工是后续独立决策，本版不铺垫常开麦克风这类隐性状态。
